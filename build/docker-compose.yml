version: '3'

services:
  feamembrane:
    build: 
        context: ../
        dockerfile: ./build/Dockerfile
        args:
          NODE_VERSION: 18.17.0
          PROJECT_NAME: FeaMembrane.Web
    ports:
    - "5000:80"
  
  
    
